services:
  bot:
    build: .
    env_file: .env
    ports:
      - "8080:8080"
    volumes:
      - ./config:/config:ro
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
